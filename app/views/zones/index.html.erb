<h1>Listing zones</h1>

<table>
  <tr>
    <th>Numero</th>
    <th></th>
    <th></th>
  </tr>

<% @zones.each do |zone| %>
  <tr>
    <td><%= zone.numero %></td>
    <td><%= zone.id %></td>
    <td><%= link_to 'Mostrar', zone %></td>
  </tr>
<% end %>
</table>

<br />

