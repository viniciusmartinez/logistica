<div class="block">         
  <div class="content">
	<div class="content-header">
    	<h2>
		<%= t("texts.contact.edit_data", :name => (link_to @contact.name, contact_path(@contact)) ).html_safe %>
		</h2>
	</div>
    <div class="inner">
        <%= render :partial => "form" %>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>